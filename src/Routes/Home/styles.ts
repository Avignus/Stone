import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  headerContainer: {
    height: 50,
  },
  container: {
    display: 'flex',
    flexDirection: 'row',
    flexWrap: 'wrap',
    paddingBottom: 50,
  },
});

export default styles;
